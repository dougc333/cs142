  handleSubmit=(e)=>{
    console.log("form handleSubmit e:",e)
    console.log("form handleSubmit e.currentTarget:",e.currentTarget)
    console.log("form handleSubmit e.target:",e.target)
    console.log("form handleSubmit e.target.value:",e.target.value)
    console.log("form handleSubmit e.target.name:",e.target.name)
    if(e.target.name==='userName'){
        console.log("e.target.username!!!!")
    }
    if(e.target.name==="password"){
        console.log("e.target.password!!!!")
    }
    console.log("event.target.childNodes[0]:",event.target.childNodes[0])
    console.log("form event.target.userName:",event.target.userName)
    console.log("form event.target.password:",event.currentTarget.password)
    
    //how to get username, password from name or value of target or currentTarget?
    //axios.post('/admin/login',formObject)
    //.then(function(response){
    //    console.log("webserver login response:",response)
    //})
    //.catch(function(err){console.log(err)})
  }
